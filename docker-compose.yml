services:
  embedding-worker:
    build: .
    ports:
      - "5201:5201"
    environment:
      - HOST=0.0.0.0
      - PORT=5201
      - MODEL_NAME=sentence-transformers/LaBSE
      - MODEL_BACKEND=onnx
      - LOG_LEVEL=INFO
      - OPENAPI_MODE=false
    restart: unless-stopped
